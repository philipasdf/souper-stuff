export class Stuff {
  id?: string;
  active: boolean;
  name: string;
  note: string;
  rating: number;
  count: number;
  price?: number;
  lastUpdated: any; // Firebase Timestamp
  tags: any; // Firebase Map
}
